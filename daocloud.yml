# This is a comment line

image: python2.7

services:
  - mysql

env:
  - MYENV=1.txt

script:
  - ls /noexist/${MYENV}
  - echo "1" > ${MYENV}
  - cat ${MYENV}
  - rm -rf ${MYENV}

notify:
  email:
    recipients:
      - haipeng.wu@daocloud.io
    on_success: off
    on_failure: off
